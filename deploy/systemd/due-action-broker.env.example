# Copy to /etc/due/action-broker.env during deployment.
# This file must not contain secrets.
BUILD_VERSION=<git-commit-sha>
NODE_ENV=production
HOME=/home/zava
PM2_HOME=/home/zava/.pm2
PATH=<directory-containing-node>:/usr/local/bin:/usr/bin:/bin
